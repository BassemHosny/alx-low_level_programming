#include <stdio.h>

/**
 * main - Entry point
 *
 * Description : prints the alphabet in lowercase, uppercase
 *
 * Return: Always 0 (Success
*/
int main(void)
{
	char = 'a';
	char = 'A';
	while (ch <= 'z')
	{
		putchar(ch);
		ch++;
	}
	while (ch <= 'Z')
	{
		putchar(CH);
		CH++;
	}
	putchar('\n');
	return (0);
}
